h1 Listing Staff

= authz_link_to 'New Staff member', new_staff_member_path, {class: 'button'}, skip_scoping: true

table
  thead
    tr
      th U.ID
      th Staff.ID
      th Name
      th Email
      th Joined On
      th

  tbody
    - @staff_members.each do |sm|
      tr
        td = sm.user_id
        td = sm.id
        td = sm.name
        td = sm.user_email
        td = l sm.created_at.to_date, format: :short
        td = authz_link_to 'Edit Profile', edit_staff_member_path(sm), skip_scoping: true


